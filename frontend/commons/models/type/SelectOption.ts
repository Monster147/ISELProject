/**
 * Opção estática de um campo `select` num formulário dinâmico.
 * @property value Valor submetido quando selecionado.
 * @property label Texto apresentado ao utilizador.
 */
export interface SelectOption {
  value: string;
  label: string;
}
